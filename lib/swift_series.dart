import 'package:flutter/material.dart';

class SwiftSeries extends StatelessWidget {
  const SwiftSeries({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Swift Series'),
      ),
      body: const Center(
        child: Text('Swift Series'),
      ),
    );
  }
}
