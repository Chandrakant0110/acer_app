import 'package:flutter/material.dart';

class AspireSeries extends StatelessWidget {
  const AspireSeries({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Aspire Series'),
      ),
      body: const Center(
        child: Text('Aspire Series'),
      ),
    );
  }
}
